#include "SortingAlgo.h"
#include "Display.h"

void Swap(short* Arr, int ia, int ib , Display* drw)
{
	int temp = Arr[ia];
	Arr[ia] = Arr[ib];
	Arr[ib] = temp;
	drw->DrawArr(Arr,)
}


void Bubble(short* Arr, short n , Display* drw)
{
	for (short i = 0; i < n; i++)
	{
		for (short j = 0; j < n - i - 1; j++)
		{

		}
	}
}

void Selection(short*, short, Display*)
{
}

void Insertion(short*, short, Display*)
{
}

void Merge(short*, short, Display*)
{
}

void Quick(short*, short, Display*)
{
}
